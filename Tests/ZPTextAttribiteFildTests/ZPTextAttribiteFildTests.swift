import XCTest
@testable import ZPTextAttribiteFild

final class ZPTextAttribiteFildTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ZPTextAttribiteFild().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
